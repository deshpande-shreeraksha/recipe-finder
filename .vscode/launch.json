{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Launch Chrome",
      "type": "msedge",
      "request": "launch",
      "url": "http://localhost:5500",  // or your actual dev server
      "webRoot": "${workspaceFolder}",
      "timeout": 20000
    }
  ]
}
